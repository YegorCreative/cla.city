{
  "private": true,
  "type": "module",
  "scripts": {
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev"
    ,"test": "node --test tests/*.test.js",
    "deploy:staging": "npm run prisma:generate && npx prisma migrate deploy && npx wrangler deploy --env staging",
    "deploy:production": "npm run prisma:generate && npx prisma migrate deploy && npx wrangler deploy --env production"
  },
  "dependencies": {
    "@prisma/client": "^6.10.0",
    "@prisma/extension-accelerate": "^2.0.2",
    "bcryptjs": "^3.0.2"
  },
  "devDependencies": {
    "prisma": "^6.10.0"
  }
}
